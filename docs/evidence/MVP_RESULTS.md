# M3 Current-code Smoke Result

Status: bounded, deterministic implementation evidence

The retained bundle in `research/output/runs/smoke/` was generated by source commit
`3d133af94655106bb7db55f6a11459b084702e98` with Python 3.12.12 on Windows.
The manifest, raw JSONL, versioned aggregate result, negative-control
counterexample, generated table, rerun command, and SHA-256 checksums are
committed together.

| Measure | Value |
|---|---:|
| Proposed actions | 2 |
| Authorised actions | 1 |
| Blocked actions | 1 |
| Executed actions | 1 |
| Provider failures | 0 |
| Negative-control counterexample length | 1 |

The table is generated from
[`research/output/runs/smoke/result.json`](../../research/output/runs/smoke/result.json), not independently
entered experimental data. The authorised fixture executes one
certificate-bound in-memory action. The attack fixture is blocked by canonical
pointwise authority/read composition. The deliberately vulnerable
`NoDefence` control produces a one-transition SLED counterexample on that
attack fixture.

Rerun from the repository root:

```text
python scripts/generate_smoke_evidence.py research/experiments/manifests/m3-smoke.yaml research/output/runs/smoke
```

This smoke run establishes pipeline readiness and deterministic evidence
generation for two scripted cases. It does not estimate real-world security or
utility, reproduce archived-paper numbers, exercise a real model, or validate
AgentDojo.
