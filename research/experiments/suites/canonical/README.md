# Canonical suite, version 1

These fixtures are corrected current-code scenarios. Independent request
Principals remain in the Principal Context, readers are policy fixture data
rather than provenance, and every Principal must independently authorise an
effect. IDs deliberately differ from the legacy reconstructions so results
cannot be silently joined as equivalent cases.

Stable fixture IDs:

- `canonical-env-01-confidential-handoff`
- `canonical-env-02-cross-project`
- `canonical-env-03-nested-assistance`
